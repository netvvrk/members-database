<%= bootstrap_form_with(model: [artwork, image]) do |f| %>
  <%= f.alert_message "Please fix the errors below." %>
  <%= f.text_field :caption, label: "Caption (optional)" %>
  <%= f.file_field :file %>
  <%= f.submit %>
<% end %>
